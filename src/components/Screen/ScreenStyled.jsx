import Box from '@mui/material/Box';
import { styled } from '@mui/system';

export const StyledBox = styled(Box)({
    height: '3rem',
    marginBottom: '4rem',
    color: 'white',
    fontSize: '3rem',
});